version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 8.0
  pre_build:
    commands:
      - echo Restore started on `date`
      - dotnet restore ./MVCApplicatiobn.csproj
  build:
    commands:
      - echo Building started on `date`
      - dotnet build ./MVCApplicatiobn.csproj
  post_build:
    commands:
      - echo Publish started on `date`
      - dotnet publish -c Release -o ./ ./MVCApplicatiobn.csproj
artifacts:
  files:
    - '**/*'